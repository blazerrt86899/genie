# Genie backend — runtime dependencies (CLAUDE.md §3)
# Install:  uv venv --python 3.12 && uv pip sync requirements.txt requirements-dev.txt
# (or plain pip:  pip install -r requirements.txt)

# ─── Web framework ───────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
gunicorn>=23.0
python-multipart>=0.0.12

# ─── Config / validation ────────────────────────────────────────────────────
pydantic>=2.9
pydantic-settings>=2.6

# ─── Database ───────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
supabase>=2.10

# ─── Redis ──────────────────────────────────────────────────────────────────
redis>=5.2

# ─── Orchestration / LLM ────────────────────────────────────────────────────
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0.6
psycopg[binary,pool]>=3.2          # checkpointer driver (session-mode, not asyncpg)
langchain-core>=0.3.20
langchain-openai>=0.2.10
langchain-groq>=0.2            # LLM_PROVIDER=groq (credit-free testing)
langchain-anthropic>=1.0       # Claude models in the picker (MODEL_CATALOG)
langchain-community>=0.3.10

# ─── Auth ───────────────────────────────────────────────────────────────────
pyjwt[crypto]>=2.10
svix>=1.40

# ─── HTTP / external services ───────────────────────────────────────────────
httpx>=0.27
tavily-python>=0.5
boto3>=1.35

# ─── File parsing + RAG ingestion (text only) ───────────────────────────────
pypdf>=5.0
pdfminer.six>=20240706         # PDF partitioning (unstructured's own "fast" backend)
unstructured>=0.15            # md/txt element typing + chunk_by_title
pgvector>=0.3                 # SQLAlchemy Vector type for document_chunks.embedding
tiktoken>=0.8                 # chunk token counts

# ─── MCP (centralized tool servers — CLAUDE.md §22) ─────────────────────────
fastmcp>=3.0

# ─── Reliability / observability ────────────────────────────────────────────
tenacity>=9.0
structlog>=24.4
