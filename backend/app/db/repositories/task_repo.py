"""TaskRepository — STUB (Phase 2). All queries filter by user_id."""

from __future__ import annotations


class TaskRepository:
    """Wire up once the Task model is active (CLAUDE.md §15 Phase 2)."""
