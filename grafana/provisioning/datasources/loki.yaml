# Auto-registers Loki as Grafana's default datasource (CLAUDE.md §21).
# Loaded on container start — no manual "Add data source" click needed.
apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
